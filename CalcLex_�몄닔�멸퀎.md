# CalcLex 프로젝트 인수인계 문서

> 새 채팅 시작할 때 이 파일을 업로드하면서 "CalcLex 사이트 계속 개발 중"이라고 알려주세요.

## 프로젝트 개요
- **사이트**: calclex.com (GitHub Pages 호스팅), 브랜드명 "CalcLex" / "계산기 사전"
- **저장소**: GitHub `everyinfo` 계정, calclex.com 커스텀 도메인 연결됨
- **사용자**: 기계공학과 졸업, 카티아 3D 설계 업무, 계산기 사이트 직접 운영 → **계산 정확성에 특히 민감**
- **언어**: 한글 86개 계산기(`/calculators/`) + 영어 78개 계산기(`/en/calculators/`)
- **영어 번역 제외 대상** (한국 문화·제도 특화): 환갑(hwangap), 금액한글변환(krwword), 만나이(age), 내신등급(grade), 수능표준점수(suneung)

## 파일 구조
```
/index.html, /en/index.html          홈페이지
/calculators/*.html                   한글 계산기 페이지 (86개)
/en/calculators/*.html                영어 계산기 페이지 (78개)
/calculators-data.js, /en/calculators-data-en.js   계산기 목록 데이터 (검색·전체목록용)
/style.css                            공용 스타일
/lang-switch.js                       언어 전환 위젯
/related-sections.js, /recent-calculators.js, /full-category-list.js   공용 위젯 (+en 버전)
```

**엔진 파일** (계산 로직, 카테고리별, 전부 -en.js 영어판 짝 있음):
`calc-engine.js`(기본계산), `elementary.js`(초등), `student.js`(학생), `engineering.js`(공학),
`logistics.js`(물류), `pet-loan.js`(반려동물나이·대출), `unit-converter-app.js`(단위변환),
`wage.js`(알바비, 영어판 없음), `everyday-en.js`(일상, 영어 전용 - 한글판은 calc-engine.js에 포함)

## ⚠️ 계산기 추가/수정 시 반드시 지켜야 할 것

### 1. 결과 공유(URL 저장·복원) — 가장 많이 놓쳤던 부분
모든 계산기는 "결과 공유하기" 버튼으로 URL에 입력값을 저장하고, 그 링크로 들어오면 자동 복원돼야 함.
- **모드 선택(버튼그룹 `cseg`)도 반드시 저장 대상 ids에 포함시킬 것** — 숫자 입력칸만 저장하고 모드(예: 파렛트/박스, 인장/전단/굽힘 등)를 빠뜨리는 실수가 정말 많았음
- 여러 모드가 있는 계산기는 **URL에서 모드값을 먼저 읽어서 초기 HTML 렌더링에 반영**한 뒤, 그 모드에 맞는 필드가 그려지게 할 것 (안 그러면 복원이 반쪽만 됨)
- 공용 `show()` 헬퍼(ST.show/EG.show/EL.show/LG.show/PC.show)들은 이제 버튼그룹(`.cseg`)의 `.on` 자식에서 `data-v`를 자동으로 읽어 저장하도록 개선됨 — 새 계산기 만들 때 이 패턴 그대로 따르면 됨
- 다국어 페이지(예: pet-loan-en.js, unit-converter-app-en.js)에서 저장 로직 자체가 아예 없거나 복원 로직이 없던 사례가 있었음 — **새 파일 만들 때 한글판에 있는 저장/복원 로직이 영어판에도 똑같이 있는지 꼭 확인**

### 2. 클립보드 복사 — 최근에 고친 버그
`navigator.clipboard.writeText().then(done)`을 **절대 단독으로 쓰지 말 것**. PC에서 네이티브 공유창을 닫으면 포커스 문제로 조용히 실패함.
반드시 각 엔진 파일 상단에 있는 `copyText()`/`legacyCopyText()` 헬퍼를 통해 호출 (구형 `execCommand('copy')` 폴백 포함). 새 엔진 파일 만들 때 이 헬퍼 두 개를 그대로 복사해서 IIFE 최상단에 넣을 것.

### 3. 저장→로드 시 필드ID 충돌 주의
같은 모드 셀렉터인데 하위 필드 id가 겹치면(예: percentkid의 'part'와 'sale' 모드가 같은 id 재사용) 모드값을 같이 저장해야 정확히 복원됨. 새 계산기 설계 시 가능하면 모드별로 필드 id를 다르게 주는 게 안전함.

### 4. GitHub Pages 배포 관련 실수 사례
- **폴더명 대소문자**: `EN` vs `en` 대소문자 달라서 전체 영어판 404 났던 적 있음 — 항상 소문자로 통일
- **`.nojekyll` 파일** 루트에 반드시 있어야 함 (없으면 Jekyll이 파일 누락시킴)
- 압축파일 드릴 때는 **실제로 다시 압축을 풀어서** `calculators-data.js`의 항목 수와 실제 파일 개수가 일치하는지, 문법 검증(`node --check`)까지 재확인하고 드릴 것

### 5. 홈페이지 타이틀 — CSS 공유 주의
한글판(`.brand-h1`)과 영어판(`.brand-lockup`)은 **완전히 분리된 클래스**로 되어 있음 (예전엔 `.home-hero h1`을 공유해서 한쪽 고치다 다른 쪽 망가뜨린 적 있음). 앞으로 홈페이지 타이틀 관련 수정 시 두 클래스가 독립적인지 항상 확인.
- 한글: `fitOneLine()` JS로 컨테이너 폭(패딩 제외) 실측 → 폰트 크기 역산 → 항상 한 줄 유지
- 영어: CALCLEX 로고 크게 + 그 폭에 맞춰 태그라인 폰트크기 자동 계산(`fitTagline()`)

### 6. 언어 스위처
- 2글자 코드 방식(`LANG_LABELS` 매핑, `ko→KO, en→EN`), 표에 없는 언어는 자동으로 앞 2글자 대문자화
- 새 언어 추가 시 `lang-switch.js`의 `LANG_LABELS` 객체에 한 줄만 추가하면 됨
- 언어 전환 버튼 클릭 시 대상 페이지 존재 여부를 HEAD 요청으로 먼저 확인 후 이동 (없으면 홈으로) — 안전장치 있음

### 7. UI 세부 수치 (건드릴 때 참고)
- nav 카테고리 탭 gap: 14px
- "더보기" 화살표(`.nav-more`) 여백: 좌우 3px
- 물류 계산기 목재(wood) 계산기: 통화 선택(USD/KRW) 가능, KRW 선택 시 참고환율로 USD 환산 병기, `/exchange-rate.json`(매일 자동 갱신) 사용

### 8. 컨테이너 적입 계산기 — 여유 공간 기능
- **박스 직접 적입 모드**: 길이·폭 방향 여유공간을 정밀하게 계산 + Top View 그림에 표시. 얇은 여유공간(화면상 8px 이하)은 인출선(점선+화살표)으로 바깥에 라벨 처리
- **파렛트 단위 모드**: 배치 알고리즘(벽돌형)이 복잡해서 정밀 도형까진 안 하고, 여유 면적·부피 숫자만 표시

## 작업 방식 (사용자가 선호하는 패턴)
- UI/디자인 변경은 **먼저 목업(HTML 파일)으로 보여주고 확인받은 뒤에 실제 코드 반영**
- 파일 2개 이상이면 **압축파일로** 전달
- "추가 작업"을 요청받으면 **이전 작업 결과물도 포함**해서 전체 압축파일로 전달
- 코드 수정 후엔 항상 `node --check`로 문법 검증 + 계산 로직은 가능하면 Node 스크립트로 시뮬레이션 검증
- 전체 사이트 전달 시: JS/CSS 재빌드(terser/cleancss) → 데이터-파일 무결성 검증(한글 86개/영어 78개, 누락 0) → sitemap.xml 최신화 → 압축 → **압축 풀어서 재검증** → 전달

## 최근 완료한 주요 작업 (시간순 최근 것)
1. 물류 계산기 컨테이너 적입 — 여유공간 표시(박스모드 정밀, 파렛트모드 숫자만) + 목재 계산기 통화선택(USD/KRW)
2. 전체 계산기 "결과 공유" 기능 감사 및 수정 (16개 엔진 파일, 한/영 전부) — 모드 선택 저장 누락, 저장/복원 로직 자체 부재 등 다수 발견 후 수정
3. 언어 스위처 존재 확인 안전장치(없는 페이지면 404 대신 홈으로)
4. 내비게이션 UI 개선 (카테고리 gap 축소, 화살표 여백 최소화, 언어 2글자 코드화)
5. 홈페이지 타이틀 반응형 처리 (한글 JS 폭맞춤, 영어 CALCLEX 로고형 레이아웃)
6. 클립보드 복사 실패 버그 수정 (16개 파일 전체, execCommand 폴백 추가)

## 아직 안 한 것 / 확인 필요할 수 있는 것
- U값 계산기의 동적 재질층(여러 겹 구조) URL 복원은 구조가 복잡해서 미적용 상태
- 애드센스 승인 상태, Google Search Console 인덱싱 진행 상황은 마지막 확인 이후 시간이 지나서 재확인 필요할 수 있음
